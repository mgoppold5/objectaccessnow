// simple program
// testing arrays and variables

public class Test4 {
	public static (t Array(tp int)) retMyArray(pdef) {
		return null;
	}

	private static (t Array(tp int)) a;

	public static (t void) main(pdef) {
		(t Array(tp int)) b;

		a = retMyArray(p);
		b = retMyArray(p);

		return;
	}
}
